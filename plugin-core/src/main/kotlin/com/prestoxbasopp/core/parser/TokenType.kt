package com.prestoxbasopp.core.parser

enum class TokenType {
    IDENTIFIER,
    NUMBER,
    STRING,
    IF,
    THEN,
    ELSE,
    ENDIF,
    WHILE,
    DO,
    ENDDO,
    RETURN,
    AND,
    OR,
    NOT,
    EQ,
    NEQ,
    LT,
    LTE,
    GT,
    GTE,
    PLUS,
    MINUS,
    STAR,
    SLASH,
    LPAREN,
    RPAREN,
    SEMICOLON,
    COMMA,
    ERROR,
    EOF,
}
