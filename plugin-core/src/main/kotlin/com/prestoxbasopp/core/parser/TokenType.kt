package com.prestoxbasopp.core.parser

enum class TokenType {
    IDENTIFIER,
    NUMBER,
    STRING,
    NIL,
    FUNCTION,
    PROCEDURE,
    ENDFUNCTION,
    ENDPROC,
    LOCAL,
    FOR,
    TO,
    STEP,
    NEXT,
    IF,
    THEN,
    ELSE,
    ENDIF,
    WHILE,
    DO,
    ENDDO,
    RETURN,
    WAIT,
    EXIT,
    QUESTION,
    AND,
    OR,
    NOT,
    EQ,
    NEQ,
    LT,
    LTE,
    GT,
    GTE,
    PLUS,
    MINUS,
    STAR,
    SLASH,
    LPAREN,
    RPAREN,
    LBRACKET,
    RBRACKET,
    LBRACE,
    RBRACE,
    SEMICOLON,
    COMMA,
    ASSIGN,
    ERROR,
    EOF,
}
